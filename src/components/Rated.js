import React from 'react';
import styled from 'styled-components';
import MovieList from '../containers/MovieList';
import Header from './Header';

const h1Style = {
    textAlign: 'center'
}

const Rated = () => {
    return (
        <div>
            <h1 style={h1Style}>Rated Movies</h1>
            
        </div>
    )
}

export default Rated;